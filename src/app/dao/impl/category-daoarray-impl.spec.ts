import { CategoryDAOArrayImpl } from './category-daoarray-impl';

describe('CategoryDAOArrayImpl', () => {
  it('should create an instance', () => {
    expect(new CategoryDAOArrayImpl()).toBeTruthy();
  });
});
